{
  "name": "xiaoyu",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "node --import tsx --test apps/web/tests/*.test.js",
    "build": "npm --workspace apps/web run build"
  },
  "devDependencies": {
    "tsx": "^4.22.1"
  }
}
