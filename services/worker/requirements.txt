# Daily report worker — Python 依赖
#
# 安装:在 services/worker/ 下执行
#   <python_bin> -m pip install -r requirements.txt
#
# 其中 <python_bin> 是 apps/web/lib/daily-report/config.js 的
# resolvePythonBinary() 解析出的 Python 解释器(优先 bundled,否则 python3)。

# RSS 采集 (services/worker/daily_report/collect.py)
feedparser>=6.0

# 现有依赖(由日报导出 / 时间解析使用,首次集中登记)
python-docx>=1.0
openpyxl>=3.1
python-dateutil>=2.9
